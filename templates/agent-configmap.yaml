---
apiVersion: v1
kind: ConfigMap
metadata:
  name: agent-config
  namespace: default
data:
  config.yaml: |
    log_level: debug
    app_kind: service
    concurrency: 5
    store_kind: serverservice
